const theme = {
  colors: {
    white: '#fff',
    black: '#000',
    primary: '#7E7EB8',
    dark: '#2C2E33',
    disable: '#747A88',
  },
};

export default theme;

export type ThemeType = typeof theme;
export type ThemeColorsType = typeof theme.colors;
